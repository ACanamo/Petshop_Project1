**PETCHUP repair plan — 17 September 2026**

**Historical plan:** Use [CRITICAL_ACTION_PLAN.md](CRITICAL_ACTION_PLAN.md) for the current implementation priorities based on [CTO_REAUDIT.md](CTO_REAUDIT.md). The plan below predates several source fixes and is retained for reference.

This plan covers all 14 findings in CTO_AUDIT.md. It describes future implementation; no application fixes or live database changes have been made. Preserve the existing React/Supabase architecture and deliver small, verifiable changes.

The deployed schema has not been inspected. First confirm whether A01 is live. If it is, contain it immediately; do not wait for the full improvement cycle. Use a short checkout maintenance window if necessary to deploy the inventory restriction and remove incompatible client fallback behavior together.

| Step | Work and audit coverage | Main files / responsibility | Completion check |
| --- | --- | --- | --- |
| 1. Establish the deployment baseline | Inspect deployed function definitions, owners, grants, policies, and schema version through authorized read-only access. Identify staging and production. Require explicit environment configuration instead of the hardcoded backend fallback. Establish a versioned migration baseline and move email-based admin bootstrap out of recurring setup. Verify a recoverable database backup before migrations. A11. | `src/lib/supabase.js`, `.env.example`, `supabase_schema.sql`, migration/configuration files. Platform + backend. | Missing production variables stop the build/startup clearly; staging targets a separate project; rerunning migrations cannot promote a demoted admin. Record source/deployment differences. |
| 2. Close the public inventory path | Revoke client execution of `deduct_product_stock` from PUBLIC, anon, and authenticated. Remove its browser caller and the fallback direct product updates. Stop creating production local-only successful orders; retain the basket and explain when checkout cannot be confirmed. A01 and immediate containment for A02. | Inventory SQL, `StoreContext.jsx`, `OrdersContext.jsx`, `CartDrawer.jsx`. Backend + frontend. | Anonymous/customer attempts to call the old stock RPC are denied. Standard `place_order` still works. Timeout or offline checkout never shows a fabricated confirmation. |
| 3. Add the regression-test foundation | Add application test scripts, database integration tests, browser journey tests, and CI using an isolated test backend. Start with access-control and checkout failures from the audit; expand alongside each later fix. A09. | `package.json`, application test directories, database tests, CI workflow. Engineering. | CI builds the app and runs allow/deny tests for anonymous, customer A, customer B, and admin. A deliberate regression fails CI. Do not run destructive fixtures against production. |
| 4. Make checkout retries safe | Add an idempotency key unique per customer and checkout attempt. Persist the key across an uncertain response and reuse it on retries. Atomically claim it in the database before stock changes; concurrent requests must return the same committed order. Bind it to the request contents and reject reuse with different contents. Provide an owner-only way to reconcile an uncertain attempt. Model submitting, confirmed, rejected, and unknown outcomes in the UI. A02. | Versioned SQL migration, `OrdersContext.jsx`, `CartDrawer.jsx`, `OrderSuccessModal.jsx`. Backend + frontend. | Concurrent identical requests create one order and deduct once. A response delayed beyond six seconds, refresh, or lost response reconciles to that same order. Changed payload with the same key is rejected. Clear cart only after confirmed success. |
| 5. Enforce stock and invoice integrity in the same transaction | Combine duplicate product IDs or reject them. Validate positive integral quantities and aggregate availability; lock products in a stable order. Check existing inventory anomalies before adding nonnegative-stock constraints; reconcile them rather than silently clamping them. Build immutable line-item names, unit prices, quantities, and amounts from catalog data on the server. Calculate totals/discounts from that snapshot. A03 and A08. | Checkout functions/triggers and constraints; `InvoiceModal.jsx`. Backend. | Stock 5 with duplicate quantities 4+4 fails without any changes. Concurrent last-item purchases yield one success. Spoofed prices/names do not enter invoice snapshots. Invoice lines and discounts reconcile to total and remain stable after catalog edits. |
| 6. Separate archival, cancellation, and returns | Replace order hard-delete controls, including bulk deletion, with archival. Remove delete-trigger restocking and restrict direct DELETE. Define allowed order transitions server-side. Permit stock restoration once for eligible unfulfilled cancellation; use a separate explicit return/restock action after delivery. Enforce the transition rules even for direct API calls. A04. | Order policies/functions/triggers, `OrdersContext.jsx`, `AdminPage.jsx`. Backend + operations. | Archiving delivered orders never changes stock. Repeated/concurrent cancellation restores once. Direct DELETE and invalid transitions fail. Order history remains available for authorized review. |
| 7. Isolate account state and align roles | Remove shared persistent order storage, including the legacy key. Prefer in-memory order caching; if persistence is necessary, scope it to immutable user ID. Clear orders and open invoice/history state on logout or account switch; discard late responses from previous sessions. Treat cloud results as authoritative. Use profile role consistently and add a separate auth-initialization state before route decisions. Remove production local-login fallback. A05 and A10. | `OrdersContext.jsx`, `AuthContext.jsx`, `ProtectedRoute.jsx`, invoice/history state. Frontend. | Admin logout followed by another user's login exposes no previous cached orders. An old in-flight request cannot repopulate them. Changing email does not transfer history. Refreshing `/admin` waits for session restoration; profile demotion removes frontend privileges. |
| 8. Preserve carts and serialize persistence | On guest-to-account login, merge the genuine guest basket with the account basket using product IDs, and persist the merged result once. Prevent a previous account's basket from becoming a guest basket. Revalidate catalog availability and prices. Inspect Supabase error results, serialize/coalesce per-item mutations, and provide a recoverable sync-failure state. Coordinate removals and cart clearing with queued writes. A06 and A13. | `CartContext.jsx`, login/checkout flow. Frontend. | A new shopper retains products through signup/login. Existing-cart duplicates merge by the documented rule. Login events/retries do not double quantities. Rapid updates, removal, and checkout clearing persist the final intended state. Sync failures are visible. |
| 9. Correct management reporting | Define metrics with the business owner: pending order value, fulfilled order value, and collected revenue are separate concepts. Until reliable payment state exists, do not label order totals as collected revenue. Calculate authorized aggregates server-side over the full relevant dataset, excluding cancelled/local-only records as appropriate. Do not recalculate historical prices from today's catalog. A07 and part of A12. | Aggregate query/function, `MetricsRibbon.jsx`, admin data loading. Backend + product. | A fixture with pending, cancelled, fulfilled, archived, and refunded records produces documented expected totals. Archival alone does not erase historical revenue. Metrics do not vary by loaded page or browser cache. |
| 10. Paginate and measure | Add bounded product/order queries with stable sorting and pagination; move necessary filtering/search server-side. Request only needed columns. Keep aggregates independent from paginated lists. Evaluate indexes from actual query plans and representative data before adding them. A12. | `StoreContext.jsx`, `OrdersContext.jsx`, shop/admin lists, database indexes if justified. Backend + frontend. | A dataset larger than the configured API row cap remains fully navigable. Pagination has no missing/duplicate rows for stable data. Filter/search works beyond page one. Record request size and render/query timings. |
| 11. Standardize accessible dialogs | Introduce a shared dialog implementation for cart, invoice, and confirmation. Set initial focus, contain Tab/Shift+Tab, prevent background interaction, support Escape, restore focus on close, and handle transitions between dialogs. A14. | Cart/invoice/success dialogs and shared component. Frontend. | Keyboard-only and screen-reader checks confirm labels, contained focus, background isolation, and appropriate focus restoration, including opening invoice from confirmation. |
| 12. Reconcile existing data and release | Inspect historical negative/mismatched stock and duplicate orders in staging copies or authorized reports. Do not assume browser-local orders were fulfilled, and do not reconstruct historical invoice prices from current prices without evidence. Resolve exceptions against fulfillment/payment records. Run the full regression suite, build, and staging journeys. Deploy compatible migrations before dependent frontend code, validate production with controlled checks, and monitor checkout/stock errors. All findings. | CI, deployment/runbook, controlled reconciliation workflow. Engineering + operations. | No unresolved P0/P1 release blockers; one order/one deduction under retries; accurate reporting; role isolation; restore procedure verified. Each finding has evidence and an owner signing off. |

**Execution dependencies:** Step 1 establishes safe targeting, but immediate A01 containment must not wait for a complete migration/tooling overhaul. Steps 2–6 form the checkout/inventory repair sequence. Step 3 starts early and receives tests with every subsequent change. Step 7 precedes Step 8 so account isolation is correct before cart merging. Step 9 follows the order-state decisions in Step 6. Step 12 follows all applicable release checks.

| Change group | Rollout and recovery rule |
| --- | --- |
| Inventory containment | Coordinate removal of the frontend fallback with server restrictions. If old clients fail, require refresh or pause checkout; do not restore public inventory mutation access. |
| Checkout API | Introduce the protected server contract before the new caller. During any compatibility window, every callable checkout path must enforce the same integrity rules. Retire obsolete overloads explicitly. |
| Constraints and data | Validate existing rows before enabling stricter constraints. Preserve evidence of corrections. Prefer forward corrective migrations; restoring a backup after new orders arrive can discard legitimate transactions. |
| Order states and metrics | Finalize cancellation/fulfillment semantics before reporting changes. Keep historical records, and document how refunds and archival affect each metric. |
| Production verification | Use controlled accounts and orders with an explicit cleanup/reconciliation procedure. A build pass alone is not release acceptance. |

**Release gate:** Close A01–A09 before real customer checkout. Move A10/A11/A13 earlier as specified because authentication, environment isolation, and reliable cart writes are dependencies of those repairs. Finish pagination and dialog improvements before broad rollout, or record a deliberate limited-release scope and owner for the remaining P2 work.

No dates are promised until deployed-state access, intended fulfillment/payment rules, and engineering capacity are confirmed. Track progress by the completion checks above rather than marking a step complete when code is merely written.
